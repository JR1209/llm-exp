stages:
  run:
    cmd: python run_experiment.py
    deps:
      - config.py
      - prompts.yaml
      - run_experiment.py
      - inputs/questions.txt
    params:
      - prompt_version
      - temperature
      - model
    outs:
      - Outputs
